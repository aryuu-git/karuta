# 产物统一输出到 data/（前端 dist、后端二进制、SQLite）
data/
*.exe
*.db
*.db-shm
*.db-wal

# 前端截图基线（视觉回归对比用，仅本地保留）
frontend/baseline/
frontend/public/ffmpeg/

# npm 依赖
frontend/node_modules/

# 本地开发脚本（dev.ps1 含密钥，严禁提交）
deploy/scripts/dev.ps1

# 环境变量 / 密钥
.env
.env.local
.env.*
